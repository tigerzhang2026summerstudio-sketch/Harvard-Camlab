Drop an optional looping backdrop video here as backdrop.mp4
(muted, any resolution; it plays very dimly behind the particles).
Without it the Cave 217 wall photo is used instead.
Change the filename in src/config/config.js -> backdrop.video.
